name: Android Build
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1

      - name: Set Up Jdk
        uses: actions/setup-java@v1
        with:
          java-version: 11


      - name: Build App
        run: ./gradlew assemble
